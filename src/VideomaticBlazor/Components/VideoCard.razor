<Card>
    @*<CardImage  Source="@Model.Thumnbnail" Alt="Placeholder image" />*@
    <CardBody>    
        <CardTitle Size="5" >            
        @Model.Title
        </CardTitle>
        <CardSubtitle Size="6">
        @Model.Description
    </CardSubtitle>
    
        <CardText>
            @*<img style="max-width: 200; height: auto" src="@Model.Thumnbnail" alt="Placeholder" /> *@
            @Model.Description
        </CardText>
                
    </CardBody>
</Card>

@code {
    [Parameter]
    public VideoDTO Model { get; set; } = new(1, "Nowhere", "No Title", "No Description");

    protected override Task OnAfterRenderAsync(bool firstRender)
    {
        return base.OnAfterRenderAsync(firstRender);
    }
}
    
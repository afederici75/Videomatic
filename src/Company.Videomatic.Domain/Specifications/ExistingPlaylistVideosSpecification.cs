using Ardalis.Specification;

namespace Company.Videomatic.Domain.Specifications;

public class ExistingPlaylistVideosSpecification : Specification<PlaylistVideo>
{
    public ExistingPlaylistVideosSpecification(PlaylistId playlistId)
    {
        //var ssd2x = from x in Query
        //        
        //        .Where(vid => vid.Id == playlistId);
    }
}